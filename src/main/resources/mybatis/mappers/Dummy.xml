<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.boot.dao.DummyDAO">

        <select id="selectAllStocks" resultType="com.boot.dto.DummyDTO">
            SELECT N, 종목명, 현재가, 전일비, 등락률,
            액면가, 시가총액, 상장주식수, 외국인비율,
            거래량, PER, ROE
            FROM popular_stocks
        </select>

        <select id="selectTopStocks" resultType="com.boot.dto.DummyDTO">
            SELECT N, 종목명, 현재가, 전일비, 등락률,
            액면가, 시가총액, 상장주식수, 외국인비율,
            거래량, PER, ROE
            FROM popular_stocks
            LIMIT #{count}
        </select>
</mapper>